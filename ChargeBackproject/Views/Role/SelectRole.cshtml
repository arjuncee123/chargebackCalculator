@model IEnumerable<ChargeBackproject.Models.UserRoles>

@{
    ViewBag.Title = "SelectRole";
}

<h2>SelectRole</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.USerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.USerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Role)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>


@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1 style="font-size:50px; font-family:'Times New Roman', Times, serif; font-style: normal; text-align: center;">Welcome to Bank</h1>
    <br />
    <div class="row">
        <div class="col-sm-4">
            <img src="~/Content/images/admin.png" width="200" height="150" />
            <br>
            <input type="radio" name="option" value="Admin">Admin <br />
        </div>
        <div class="col-sm-4">
            <img src="~/Content/images/customer.jpg" width="200" height="150" />
            <br>
            <input type="radio" name="option" value="Customer">Customer <br />
        </div>
        <div class="col-sm-4">
            <img src="~/Content/images/employee.jpg" width="200" height="150" />
            <br>
            <input type="radio" name="option" value="Employee">Employee
        </div>
    </div>
    <div class="row">
        <br />
        <br />
        <div style="text-align: center;">
            <input type="submit" class="btn btn-success" value="Continue" onclick="@("window.location.href='" + @Url.Action("Login", "Account") + "'");" />        </div>
    </div>
</div>    


